language: node_js
node_js:
  - "10"
script:
  - npm run lint
  - npm run build
deploy:
    provider: pages
    skip_cleanup: true
    github_token: $github_token
    local_dir: dist/ng-web-apis
    on:
        branch: master
